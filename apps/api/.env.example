# Use your own PostgreSQL URL. Never put this in the mobile app.
DATABASE_URL=postgresql://thekabook:local_dev_only@localhost:5432/thekabook
PORT=4000
CORS_ORIGINS=http://localhost:8081,http://localhost:19006
# development-only embedded PostgreSQL alternative; do not use for hosting
# DATABASE_DRIVER=pglite
# PGLITE_PATH=./.data/thekabook
